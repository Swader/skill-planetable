# Claude Usage

This repository is a Claude Agent Skill because the root `SKILL.md` follows the Agent Skills format. To use it as a Claude Code project skill from this checkout, keep the `.claude/skills/planetable` symlink bundle intact; to use it globally, link or copy this folder to `~/.claude/skills/planetable`.
